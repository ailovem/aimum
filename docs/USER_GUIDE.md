# AImum 用户手册

## 快速开始

### 1. 访问 AImum

打开浏览器访问：https://your-domain.vercel.app

### 2. 注册/登录

1. 输入手机号
2. 点击"获取验证码"
3. 输入验证码
4. 完成登录

> 💡 新用户自动获得 **100 tokens** ！

---

## 功能介绍

### 💬 AI 对话

在对话页面，你可以：

- **选择模型**: Claude / GPT-4 / DeepSeek
- **选择角色**: 默认助手 / 代码专家 / 写作助手 / 数据分析师
- **发送消息**: 输入内容，按 Enter 发送

### 💎 Token 管理

查看和管理你的 Token：

- **余额查询**: 查看剩余 tokens
- **充值**: 选择套餐进行充值
- **消费记录**: 查看使用历史

| 套餐 | 价格 | Tokens | 特点 |
|------|------|--------|------|
| 免费版 | ¥0 | 100 | 体验用 |
| 专业版 | ¥29/月 | 5000 | 日常用 |
| 企业版 | ¥99/月 | 20000 | 商业用 |

### 🧩 插件市场

安装插件扩展功能：

- **文件转换专家**: PDF/Word/Excel 互转
- **OCR 文字识别**: 图片文字提取
- **网络搜索**: 实时搜索
- **计算器**: 数学计算
- **代码运行器**: 运行代码

### 📊 进度追踪

查看项目开发进度：

1. 打开进度页面
2. 点击任务完成打勾
3. 自动更新进度条

---

## API 文档

### 认证接口

```
POST /api/auth/send-code
POST /api/auth/login
POST /api/auth/register
GET  /api/auth/me
```

### 对话接口

```
POST /api/chat/chat
GET  /api/chat/history
GET  /api/chat/models
GET  /api/chat/roles
```

### Token 接口

```
GET  /api/tokens/me
POST /api/tokens/consume
POST /api/tokens/recharge
GET  /api/tokens/plans
```

### 插件接口

```
GET  /api/plugins/plugins
POST /api/plugins/install
POST /api/plugins/uninstall
GET  /api/plugins/installed
```

---

## 常见问题

### Q: Token 有什么用？

A: 每发送一条消息会消耗 10-20 tokens，不同模型消耗不同。

### Q: 如何充值？

A: 登录后进入 Token 管理页面，选择套餐并完成支付。

### Q: 支持哪些模型？

A: Claude Sonnet 4、Claude Haiku 3、GPT-4o、GPT-4o Mini、DeepSeek Chat。

### Q: 可以离线使用吗？

A: 是的！AImum 支持 PWA，可以安装到桌面/主屏幕，离线也能使用部分功能。

---

## 快捷命令

### 开发命令

```bash
npm run dev          # 本地开发
npm run test         # 运行测试
npm run tracker      # 查看进度
npm run smart        # 智能启动
```

### 同步命令

```bash
node scripts/auto-sync.js status  # 查看同步状态
node scripts/auto-sync.js sync    # 手动同步
```

---

## 项目信息

- **版本**: 1.0.0
- **GitHub**: https://github.com/ailovem/aimum
- **状态**: Phase 1 MVP 完成

---

## 反馈

如有问题，请通过以下方式反馈：

- GitHub Issues
- OpenClaw 反馈

---

**💡 提示**: 记得定期查看进度页面，点击完成的任务可以打勾！
